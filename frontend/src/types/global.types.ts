import { Header } from "@/types/layout/header.types";
import { Footer } from "@/types/layout/footer.types";

export type Global = {
  title: string;
  description: string;
  header: Header;
  footer: Footer;
};
